# Three pillars — hackathon pitch

**Vision:** *The agent that onboard itself — then executes onchain via KeeperHub.*

| # | Pillar | One line | Entry |
|---|--------|----------|-------|
| 1 | [Self-registering Agent](./SELF_ONBOARDING_AGENT.md) | Headless SIWE → `kh_` → first tx | `npm run bootstrap` |
| 2 | [Audit-first Last Mile](./AUDIT_FIRST.md) | simulate → execute → verified receipts | `npm run demo` |
| 3 | [Dual MCP Execution Layer](./DUAL_MCP.md) | local stdio + official hosted MCP | `docs/mcp-dual.json.example` |

## For judges (60 seconds)

1. **Pillar 1:** Show `npm run bootstrap` log — agent registers without browser  
2. **Pillar 2:** Run `npm run demo` — treasury plan prints audit trail + tx link  
3. **Pillar 3:** Open `docs/mcp-dual.json.example` — copy both MCP servers into Cursor  

## BUIDL one-liner

```
Self-onboarding agent + audit-first execution + dual MCP — real Base Sepolia tx via KeeperHub.
```
